apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: node-web
  ports:
    - protocol: TCP
      port: 80       # Port của Service
      targetPort: 3000 # Port của App (khớp với Dockerfile)
  type: NodePort # Hoặc LoadBalancer nếu bạn dùng Cloud