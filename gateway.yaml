apiVersion: gateway.networking.k8s.io/v1beta1
kind: Gateway
metadata:
  name: my-app-gateway
  namespace: default
spec:
  gatewayClassName: gke-l7-gxlb # Class chuẩn cho Load Balancer của Google
  listeners:
  - name: http
    protocol: HTTP
    port: 80
    allowedRoutes:
      namespaces:
        from: Same